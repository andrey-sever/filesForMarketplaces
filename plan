Работа через Pandas

Процедура Главная() 
    ПереборФайлов()
        списокНужныхСтолбцов = ПолучитьНужныеСтолбцы(имяПутьФайла)
        перТекущаяОтфильтрованнаяТаблица = ПолучитьТаблицуСНужнымиСтолбцами(списокНужныхСтолбцов, имяПутьФайла)
        //добавляем в общую таблицу
    //фильтруем: по остатку, если нужно
    //сохраняем файл
 
Функция ПолучитьНужныеСтолбцы(имяПутьФайла)
    перЧейФайл = ОпределитьЧейФайл(имяПутьФайла)
    Возврат списокИзКонфФайла(перЧейФайл)
    
Функция ОпределитьЧейФайл(имяПутьФайла)
    //Определяем файл по шапке таблицы
    
Функция СписокИзКонфФайла(перЧейФайл)
    //Получаем список из конфигурационного файла
    
    
Функция ПолучитьТаблицуСНужнымиСтолбцами(списокНужныхСтолбцов, имяПутьФайла)
    перТекущаяТаблица = ПолучитьТаблицу(имяПутьФайла)    
    // из перТекущаяТаблица сделать таб. с нужными столб. и вернуть
    
Функция ПолучитьТаблицу(имяПутьФайла) //Попробовать сделать через класс
    //Получение таблицы
